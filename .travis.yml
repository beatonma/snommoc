dist: xenial
language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pip install -r requirements.txt

before_script:
  - python manage.py makemigrations --settings=basetest.test_settings_default
  - python manage.py migrate --settings=basetest.test_settings_default

script:
  - python runtests.py
